# setup a service for holding a tunnel to SSH server

* copy the key file to `/etc/default/<target name>`, I use `aliyun` at the moment
* have to run the `ssh` script with `sudo` first to let the OS remember the key
